/** @format */

import styled from 'styled-components';

const StyledAsideProfile = styled.div`
  display: flex;
  flex-direction: column;
  justify-content: space-between;
`;

const AsideProfile = () => {
  return <StyledAsideProfile></StyledAsideProfile>;
};

export default AsideProfile;
